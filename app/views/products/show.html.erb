<div class="container">
  <div class="row mt-5">
    <div class="col-sm-12">
      <h1><%= @product.name %></h1>
      <p><%= @product.description %></p>
      <a href="<%= @product.url %>" target="_blank">Get it now!</a>
    </div>
  </div>
</div>
